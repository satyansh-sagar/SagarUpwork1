using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace Upwork1.Constants
{
    public class Enums
    {
        public enum Greeting
        {
            greet,sendoff
        };

        public enum Email
        {
            sendEmail
        };

        public enum Help
        {
            help
        };

        public enum Slangs
        {
            goodWords,badWords
        };

        public enum MarketShare
        {
            share,location,brand,category,msl
        };
    }
}